import sys
# we can investigate the platform on which python is running
# especially useful if we run on a different platform to where we develop
# maxsize tells us the largest memory-resident object we may use
# print(sys.path, sys.maxsize, sys.version_info)

print(10**10000000)